[project]
name = "no-more-ravines"
version = "0.2.1"
description = "A datapack that disables natural ravine generation in the overworld."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "beet>=0.115.0",
]
